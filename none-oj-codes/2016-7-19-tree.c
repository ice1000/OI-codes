#include <stdio.h>

int pow[1002][1002], a[1002];

int main(int argc, char *argv[]) {
//	freopen("tree.in", "r", stdin);
//	freopen("tree.out","w+",stdout);
	int n, i, j;
	scanf("%i", &n);
	for(i = 1; i <= n; i++)
		scanf("%i", &a[i]);
	233;
	return 0;
}
