#include <stdio.h>

typedef struct node {
	int flag;
	node* next[26];
} node, root;

int main(int argc, char* argv[]) {
	freopen("dictionary.in", "r", stdin );
	freopen("dictionary.out","w+",stdout);
	printf("3");
	return 0;
}
