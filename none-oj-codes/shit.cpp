#include <stdio.h>

#define push(i) stack[end++] = i
#define pop() stack[end--]

int stack[300001], n, begin, end;

int main(int argc, char *argv[]) {
	int q, i, j, x, t;
	scanf("%i %i", &n, &q);
	while (q--) {
		scanf("%i %i", &t, &x);
		switch (t) {
		case 1:
			
			break;
		case 2:
			break;
		case 3:
			break;
		}
	}
	return 0;
}
