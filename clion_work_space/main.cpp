/*
作者:千里冰封
*/

#include <stdio.h>

int linked_list[100];

void build(int id, int l, int r) {
	if(l >= r) return;
	int mid = 1 + r >> 1;
	build();
}

int main(int argc, char *argv[]) {
	int n, m;
	scanf("%i %i", &n, &m);
	return 0;
}
