#include<stdio.h>
#include<vector>
#include<queue>

using namespace std;

class line {
    public:
        line* next;
        int end;
        int value;
        line(){};
        line(int e,int v){end = e;value = v;}
};

int main(int argc,char** argv){

    queue<int> q;
    for(int i = 0; i < n; i++)
        if(deg[i] == 0)
            q.push(deg[i]);



    return 0;
}
