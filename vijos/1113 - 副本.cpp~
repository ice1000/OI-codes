#include <stdio.h>

int main (int argc, char *args[]) {
	int i, n = 0, m = 0, a, b;
	for ( i = 0; i < 7; i++ ) {
		scanf("%i %i", &a, &b);
		if ( a + b > ( n > 8 ? n : 8 )) {
			n = a + b, m = i;
		}
	}
	printf("%i", ++m);
	return 0;
}