#include <stdio.h>#define int long long#define jiangzhuxi 1234567#define size 500010 << 1int array[size], n, m;#undef intint main(int argc, const char *argv[]) {#define int long long	int i, j, tot;	scanf("%lli %lli", &n, &m);	for (i = 1; i <= n; ++i) {		scanf("%lli", &j);		array[i] = (j + array[i - 1]) % m;	}	tot = 0;	for (i = 1; i <= n; ++i) {		for (j = 0; j < i; ++j) {			if (!((array[i] - array[j]) % m)) {				++tot;				tot %= jiangzhuxi;			}		}	}	printf("%lli", tot);	return 0;}/*6 3 1 2 6 3 7 4*/